version: '3'
services:
  db:
    build:
      context: .
      dockerfile: ./postgres/Dockerfile
  web:
    build: ./
  mqtt:
    image: eclipse-mosquitto:latest
  nginx:
    image: nginx:latest
  web_test:
    build:
      context: .
      dockerfile: ./tests/Dockerfile
